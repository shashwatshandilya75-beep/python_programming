limit = int(input("Enter a number to sum up to: "))
total_sum = 0

# The range function starts at 1 and goes up to (limit + 1)
for i in range(1, limit + 1):
    total_sum += i

print(f"The sum of all numbers from 1 to {limit} is: {total_sum}")
